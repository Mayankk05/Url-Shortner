spring.application.name=UrlShortener

# COMPLETE application.properties with ALL required properties

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/urlshortener?useSSL=false&serverTimezone=UTC&createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=data@base
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration (REQUIRED - these were missing!)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Redis Configuration
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.password=Riyank@123
spring.redis.jedis.pool.max-active=8
spring.redis.jedis.pool.max-idle=8
spring.redis.jedis.pool.min-idle=0

# Jackson Configuration
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.serialization.write-durations-as-timestamps=false
spring.jackson.serialization.fail-on-empty-beans=false
# Reduce SQL logging (only show it when needed)

logging.level.org.hibernate.SQL=WARN

# Enable caching
spring.cache.type=simple
# JWT Configuration
app.jwt.secret=mySecretKey1234567890123456789012345678901234567890
app.jwt.expiration=86400000


# Disable default static resource handling to avoid conflicts
spring.web.resources.add-mappings=false

# Logging to help debug
logging.level.org.springframework.web=DEBUG
logging.level.org.springframework.security=DEBUG
# Application Configuration
app.base-url=http://localhost:8080

# Server Configuration
server.port=8080
#nlqd lvcf dmsr stpt



spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=creationbusinesscontent@gmail.com
spring.mail.password=nlqd lvcf dmsr stpt
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


# Enable debug logging to see all registered endpoints
logging.level.org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping=DEBUG
